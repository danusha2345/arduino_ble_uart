═══════════════════════════════════════════════════════════════════════════════
  ESP32-S3-Zero PIN WIRING DIAGRAM - Optimized Layout
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│                         GPS MODULE (UART0)                                  │
│                          GPIO43/44 Pins                                     │
└─────────────────────────────────────────────────────────────────────────────┘

    ESP32-S3-Zero              GPS Module
    ┌────────────┐            ┌──────────┐
    │   GPIO44   │────RX─────>│    TX    │  
    │   GPIO43   │<───TX──────│    RX    │
    │    GND     │────────────│   GND    │
    │   3.3V     │────────────│   VCC    │
    └────────────┘            └──────────┘

    Примечание: UART0 выведен на плате ESP32-S3-Zero
    USB CDC отключен для освобождения этих пинов


┌─────────────────────────────────────────────────────────────────────────────┐
│                    OLED DISPLAY (I2C)                                       │
│                       GPIO1-2 Pins                                          │
└─────────────────────────────────────────────────────────────────────────────┘

    ESP32-S3-Zero              OLED Display (SSD1306)
    ┌────────────┐            ┌──────────┐
    │   GPIO1    │────SCL─────│   SCL    │
    │   GPIO2    │────SDA─────│   SDA    │
    │    GND     │────────────│   GND    │
    │   3.3V     │────────────│   VCC    │
    └────────────┘            └──────────┘

    I2C Address: 0x3C (or 0x78)


┌─────────────────────────────────────────────────────────────────────────────┐
│                    TFT DISPLAY (SPI)                                        │
│                      GPIO9-13 Pins                                          │
└─────────────────────────────────────────────────────────────────────────────┘

    ESP32-S3-Zero              TFT Display (ST7789V)
    ┌────────────┐            ┌──────────┐
    │   GPIO9    │────DC──────│    DC    │  Data/Command
    │   GPIO10   │────RST─────│   RST    │  Reset
    │   GPIO11   │────MOSI────│   SDA    │  SPI Data
    │   GPIO12   │────SCLK────│   SCL    │  SPI Clock
    │   GPIO13   │────BL──────│    BL    │  Backlight
    │    GND     │────CS──────│    CS    │  Chip Select (to GND)
    │    GND     │────────────│   GND    │
    │   3.3V     │────────────│   VCC    │
    └────────────┘            └──────────┘

    Примечание: CS подключен к GND на дисплее
    Если подсветка всегда включена, GPIO13 -> 3.3V


┌─────────────────────────────────────────────────────────────────────────────┐
│                      RGB LED (Built-in)                                     │
└─────────────────────────────────────────────────────────────────────────────┘

    ESP32-S3-Zero
    ┌────────────┐
    │   GPIO21   │ ──────> WS2812 RGB LED (на плате)
    └────────────┘

    ⚠️ GPIO21 занят встроенным светодиодом!


┌─────────────────────────────────────────────────────────────────────────────┐
│                     RESERVED PINS (DO NOT USE)                              │
└─────────────────────────────────────────────────────────────────────────────┘

    ⛔ GPIO21       - WS2812 RGB LED (встроенный)
    ⛔ GPIO33-37    - Octal PSRAM (зарезервированы системой)
    ⛔ GPIO43-44    - USB Serial/JTAG (UART0 для USB CDC отладки)


┌─────────────────────────────────────────────────────────────────────────────┐
│                      FREE PINS (Available)                                  │
└─────────────────────────────────────────────────────────────────────────────┘

    ✅ GPIO0, GPIO1, GPIO2, GPIO3, GPIO4
    ✅ GPIO14, GPIO15, GPIO16, GPIO17, GPIO18, GPIO19, GPIO20
    ✅ GPIO38, GPIO39, GPIO40, GPIO41, GPIO42
    ✅ GPIO45, GPIO46, GPIO47, GPIO48


┌─────────────────────────────────────────────────────────────────────────────┐
│                      USB CDC DEBUG (Built-in)                               │
└─────────────────────────────────────────────────────────────────────────────┘

    ✅ USB CDC включен для отладки через встроенный USB!
    
    ESP32-S3-Zero
    ┌────────────┐
    │ USB-C Port │ ──────> COM порт для Serial.print() отладки
    └────────────┘
    
    GPIO43/44 используются для USB Serial/JTAG (UART0)
    
    Подключение:
    1. Подключите USB-C кабель к плате
    2. Откройте Serial Monitor: platformio device monitor -e esp32-s3 -b 115200
    3. Используйте Serial.print() для отладки


═══════════════════════════════════════════════════════════════════════════════
                              SUMMARY
═══════════════════════════════════════════════════════════════════════════════

✅ GPS:        GPIO5(RX), GPIO6(TX) - UART1
✅ OLED:       GPIO1(SCL), GPIO2(SDA) - I2C
✅ TFT:        GPIO9-13 (DC,RST,MOSI,SCLK,BL) - SPI (временно отключен)
✅ LED:        GPIO21 - WS2812 (встроенный)
✅ DEBUG:      GPIO43-44 - USB CDC Serial (встроенный)
✅ WiFi AP:    UM980_GPS_BRIDGE_S3 (192.168.4.1)
✅ BLE:        UM980_S3_GPS
✅ BLE UUID:   6E400001-B5A3-F393-E0A9-E50E24DCCA9E (стандартный NUS)
⛔ Reserved:  GPIO21, GPIO33-37, GPIO43-44
📝 Free:      GPIO0, GPIO3-4, GPIO7-8, GPIO14-20, GPIO38-42, GPIO45-48

═══════════════════════════════════════════════════════════════════════════════
